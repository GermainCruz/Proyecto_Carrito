PORT=4000
FRONTEND_URL=http://localhost:5173
FRONTEND_URLS=http://localhost:5173,http://localhost:5174
SESSION_SECRET=dev_secret_change_me
SESSION_SAMESITE=lax
SESSION_SECURE=false

# Conexion via URL (Render + Neon)
DATABASE_URL=
DATABASE_SSL=true

# Conexion local (opcional si no se usa DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=carrito_compras
DB_USER=postgres
DB_PASSWORD=sa

TAX_RATE=0.18
